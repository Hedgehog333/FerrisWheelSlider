(function( $ ) {
    $.fn.ferrisWheelSlider = function(option) {
        var setting = $.extend({
            arrows: true,
            autoPlay: true,
            duration: 5000,
            radius: 600,

        };

        var make = function(){
            
        };

        return this.each(make);
    };
})(jQuery);